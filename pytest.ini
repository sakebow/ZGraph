[pytest]
# pytest discovers tests under tests/ by default. Markers are declared here
# to silence PytestUnknownMarkWarning and to allow selective runs via
# ``pytest -m <marker>``.
testpaths = tests
markers =
    integration: integration tests that exercise the configured runtime end-to-end
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
